FILES:

# cnn-svm-rf(1)			-	Contains CNN+SVM+RF trained on the whole dataset and google generated pictures (the output is in the ouput folder)

# svm_without_cnn_last_layer	-	Contains SVM without any other model. The accuracy was less, hence did not work on the result (NO OUTPUT)